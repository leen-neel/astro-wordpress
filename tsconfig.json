{
    "extends": "astro/tsconfigs/base",
    "compilerOptions": {
        // Needed for TypeScript intellisense in the template inside Vue files
        "jsx": "preserve",
        "baseUrl": ".",
        "allowJs": true,
        "types": ["astro/client"],
        "paths": {
            "~/*": ["src/*"],
            "lib/*": ["lib/*"]
        }
    }
}
